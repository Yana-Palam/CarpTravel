export type ServicesSlideProps = {
  className?: string;
  total: number;
  active: number;
  picture: any;
  bg: any;
  title: string;
  description: string;
  onClickBtn: (index: number) => void;
};
